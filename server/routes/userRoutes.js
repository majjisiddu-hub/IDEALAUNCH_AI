const router = require("express").Router(); const controller = require("../controllers/userController"); const auth = require("../middleware/authMiddleware"); router.use(auth); router.get("/profile", controller.profile); router.put("/profile", controller.updateProfile); router.get("/favourites", controller.favourites); router.post("/favourites/:ideaId", controller.addFavourite); router.delete("/favourites/:ideaId", controller.removeFavourite); router.get("/recently-viewed", controller.recentlyViewed); router.post("/recently-viewed/:ideaId", controller.addRecentlyViewed); module.exports = router;
