"use client";
import Link from "next/link";
export default function ProblemCard({ problem }) { return <article className="idea-card problem-card"><div className="idea-card-topline"><span className="category-pill">{problem.category}</span><span className={`severity-badge severity-${String(problem.severity).toLowerCase()}`}>{problem.severity}</span></div><span className="community-badge">REAL-TIME PROBLEM</span><h3>{problem.title}</h3><p>{problem.description}</p><div className="problem-meta"><span>{problem.location}</span><span>{problem.affectedPeople}</span></div><p className="problem-opportunity"><strong>Opportunity:</strong> {problem.opportunity}</p><div className="tag-list">{(problem.tags || []).map((tag) => <span key={tag}>#{tag}</span>)}</div><Link className="button button-secondary card-action" href={`/problems/${problem.id}`}>View details <span>↗</span></Link></article>; }
