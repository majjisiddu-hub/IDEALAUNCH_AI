"use client";
import Link from "next/link"; import { useEffect, useState } from "react"; import AppShell from "../../components/AppShell"; import IdeaCard from "../../components/IdeaCard"; import StatCard from "../../components/StatCard"; import { getAllIdeas, getIdeasByIds, titleOf } from "../../lib/ideas"; import { getFavouriteIds, getUsername, getViewedIds, getUserIdeas } from "../../lib/storage"; import { api } from "../../lib/api";
export default function DashboardPage() { const [data, setData] = useState({ username: "Explorer", ideas: [], recent: [], saved: 0, viewed: 0, mine: 0, total: 0, categories: 0 }); useEffect(() => { const fallback = getAllIdeas(); const load = async () => { try { const [ideaResult, favouriteResult, viewedResult] = await Promise.all([api.get("/ideas"), api.get("/users/favourites"), api.get("/users/recently-viewed")]); const all = ideaResult.ideas; setData({ username: getUsername(), ideas: all.slice(0, 3), recent: viewedResult.ideas, saved: favouriteResult.ideas.length, viewed: viewedResult.ideas.length, mine: all.filter((idea) => idea.createdByUserId === sessionStorage.getItem("userId")).length, total: all.length, categories: new Set(all.map((idea) => idea.category)).size }); } catch { setData({ username: getUsername(), ideas: fallback.slice(0, 3), recent: getIdeasByIds(getViewedIds()), saved: getFavouriteIds().length, viewed: getViewedIds().length, mine: getUserIdeas().filter((idea) => idea.createdBy === getUsername()).length, total: fallback.length, categories: new Set(fallback.map((idea) => idea.category)).size }); } }; load(); }, []); return <AppShell><header className="page-header"><div><span className="eyebrow">Thursday, September 10, 2026</span><h1>Good to see you, {data.username}.</h1><p>Your launch queue is looking focused. Keep following the signal.</p></div><Link className="button button-primary" href="/ideas">Explore ideas <span>→</span></Link></header><section className="stat-grid"><StatCard label="Total ideas" value={data.total} note="Curated library" /><StatCard label="Saved ideas" value={data.saved} note="Your shortlist" /><StatCard label="Categories" value={data.categories} note="Markets to scan" /><StatCard label="Recently viewed" value={data.viewed} note="Keep exploring" /><StatCard label="Your Ideas" value={data.mine} note="Your contributions" /></section><section><div className="section-heading"><div><span className="section-label">Selected for you</span><h2>Featured ideas</h2></div><Link className="text-link" href="/ideas">View all ideas →</Link></div><div className="idea-grid">{data.ideas.map((idea) => <IdeaCard key={idea.id} idea={idea} />)}</div></section><section className="recent-layout"><div><div className="section-heading"><div><span className="section-label">Your trail</span><h2>Recently viewed</h2></div><Link className="text-link" href="/ideas">Browse library →</Link></div><div className="recent-list">{data.recent.length ? data.recent.slice(0, 3).map((idea) => <Link className="recent-item" key={idea.id} href={`/ideas/${idea.id}`}><span className="recent-item-icon">{titleOf(idea).charAt(0)}</span><span><strong>{titleOf(idea)}</strong><small>{idea.category}</small></span><span className="arrow">↗</span></Link>) : <div className="recent-empty"><span className="empty-icon small">↗</span><p>Your viewed ideas will appear here.</p><Link href="/ideas">Browse the library</Link></div>}</div></div><div><div className="section-heading"><div><span className="section-label">Quick moves</span><h2>Make progress</h2></div></div><div className="recent-list"><Link className="recent-item" href="/ideas"><span className="recent-item-icon">+</span><span><strong>Scan the library</strong><small>Find a new direction</small></span><span className="arrow">↗</span></Link><Link className="recent-item" href="/favourites"><span className="recent-item-icon">♡</span><span><strong>Review favourites</strong><small>Shape your shortlist</small></span><span className="arrow">↗</span></Link></div></div></section><section className="ai-dashboard-card"><div><span className="ai-spark">✦</span><div><span className="section-label">IDEALAUNCH AI</span><h2>Your next thinking partner.</h2><p>Analyze ideas, discover opportunities, find business models and plan your launch.</p></div></div><Link className="button button-primary" href="/ai-helper">Ask AI <span>→</span></Link></section></AppShell>; }
