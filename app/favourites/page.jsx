"use client";
import Link from "next/link"; import { useEffect, useState } from "react"; import AppShell from "../../components/AppShell"; import IdeaCard from "../../components/IdeaCard"; import EmptyState from "../../components/EmptyState"; import { getIdeasByIds } from "../../lib/ideas"; import { getFavouriteIds } from "../../lib/storage"; import { api } from "../../lib/api";
export default function FavouritesPage() { const [items, setItems] = useState([]); useEffect(() => { api.get("/users/favourites").then((result) => setItems(result.ideas)).catch(() => setItems(getIdeasByIds(getFavouriteIds()))); }, []); return <AppShell><header className="page-header"><div><span className="eyebrow">Your shortlist / Saved</span><h1>Ideas with pull.</h1><p>Keep the concepts that deserve another conversation close by.</p></div><Link className="button button-primary" href="/ideas">Find more ideas <span>→</span></Link></header><div className="idea-grid">{items.length ? items.map((idea) => <IdeaCard key={idea.id} idea={idea} />) : <EmptyState title="No saved ideas yet" message="Save promising concepts from the Ideas library and they will appear here." />}</div></AppShell>; }
